# AI 入口

## 目标

本仓库默认用于读取“经过结构化整理的原始口述材料”。首要任务是忠实整理和提取材料，而不是延续旧分析或提前固化结论。

如果需要分析，应把分析视为单独产物，不要和事实层混写。

## 默认读取顺序

1. `AI-ENTRY.md`
2. `profile/baseline.md`
3. `evidence/raw-log.md`
4. 根据需要再读 `evidence/episodes/`
5. 只有用户明确要求参考旧判断时，才读 `analysis/`

`README.md` 只在需要理解仓库方法和边界时再读，不作为默认入口。

## 读取要求

- 优先提取时间、背景、任务、动作、结果、反馈、原话和不确定点。
- 不要把 `analysis/` 里的旧判断直接当成事实。
- 如果原始卡片里混有少量旧分析，应优先保留其中的事实部分，并明确哪些内容属于推测。
- 用户没明确要求分析时，默认只做结构化整理、归纳和对齐，不扩展解释。

## 什么更接近可用材料

- 具体任务中的真实动作
- 可追溯的结果、反馈、返工和对比
- 口述中的原话、时间点、对象和上下文
- 明确写出的不确定点和缺失信息

## 什么不应直接上升为结论

- 单纯喜欢或排斥
- 抽象人格标签
- 一次偶然成功
- 没有外部反馈或结果支撑的印象式描述
- 旧分析文件中的候选优势名称

## 输出要求

1. 先提炼事实。
2. 再标注不确定点或缺口。
3. 如果用户明确要求分析，把分析单列成独立部分。
4. 不要把未被明确说出的内容补写成事实。

材料不足时，直接说“目前材料只够整理，不够下结论”。
