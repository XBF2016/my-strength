# 分析层说明

`analysis/` 用来存放某一阶段的解释、假设、优势地图和阶段性判断。

这里的内容有几个边界：

- 它们不是事实层，也不是默认入口。
- 它们依赖于当时可见的材料、提示词和模型能力。
- 它们应该被视为可更新、可丢弃、可重算的中间产物。
- 如果和 `profile/`、`evidence/` 的原始资料冲突，优先回到原始资料。

默认情况下，新的 AI 或新的分析流程应先读取原始资料层，再决定是否参考这里的旧分析。
